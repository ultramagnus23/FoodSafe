# FoodSafe India — Requirements (API + Pipeline)
# pip install -r requirements.txt

# ---- API (FastAPI backend) ----
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
asyncpg>=0.29.0           # async PostgreSQL driver used by api/db.py
pydantic[email]>=2.7.0    # EmailStr needs the email-validator extra
PyJWT>=2.8.0              # JWT create/verify in api/auth_utils.py
bcrypt>=4.1.0             # password hashing in api/auth.py
python-dotenv>=1.0.0      # loads .env (DATABASE_URL, JWT_SECRET) for local/Supabase

# ---- Core ----
psycopg2-binary>=2.9.9    # sync driver used by the pipeline (models/, pipeline/)
httpx>=0.27.0
boto3>=1.34.0

# ---- PDF / OCR ----
pdf2image>=1.17.0          # PDF pages → PIL images (needs poppler-utils OS package)
pytesseract>=0.3.13        # Tesseract wrapper (needs tesseract-ocr OS package)
pdfplumber>=0.11.0         # Table extraction from PDFs
Pillow>=10.3.0

# ---- NLP / NER ----
spacy>=3.7.0
# Also run: python -m spacy download en_core_web_sm

# ---- Text matching ----
rapidfuzz>=3.9.0           # Fast fuzzy string matching

# ---- HTML / RSS parsing ----
beautifulsoup4>=4.12.0
lxml>=5.2.0

# ---- Headless browser (FoSCoS recall scraper) ----
playwright>=1.44.0         # also run: python -m playwright install chromium

# ---- Date parsing ----
python-dateutil>=2.9.0
dateparser>=1.2.0

# ---- Scheduling ----
apache-airflow>=2.9.0
celery[redis]>=5.4.0

# ---- Data / ML (Stage models) ----
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
networkx>=3.3.0            # Supply chain DAG
statsmodels>=0.14.0        # Granger causality

# ---- Testing ----
pytest>=8.0.0
pytest-cov>=5.0.0
responses>=0.25.0          # Mock HTTP in tests

# ---- Monitoring ----
structlog>=24.0.0
sentry-sdk>=2.0.0

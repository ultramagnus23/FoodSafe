# FoodSafe India — Pipeline Requirements
# pip install -r requirements.txt

# ---- Core ----
psycopg2-binary>=2.9.9
httpx>=0.27.0
boto3>=1.34.0

# ---- PDF / OCR ----
pdf2image>=1.17.0          # PDF pages → PIL images (needs poppler-utils OS package)
pytesseract>=0.3.13        # Tesseract wrapper (needs tesseract-ocr OS package)
pdfplumber>=0.11.0         # Table extraction from PDFs
Pillow>=10.3.0

# ---- NLP / NER ----
spacy>=3.7.0
# Also run: python -m spacy download en_core_web_sm

# ---- Text matching ----
rapidfuzz>=3.9.0           # Fast fuzzy string matching

# ---- HTML / RSS parsing ----
beautifulsoup4>=4.12.0
lxml>=5.2.0

# ---- Date parsing ----
python-dateutil>=2.9.0
dateparser>=1.2.0

# ---- Scheduling ----
apache-airflow>=2.9.0
celery[redis]>=5.4.0

# ---- Data / ML (Stage models) ----
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
networkx>=3.3.0            # Supply chain DAG
statsmodels>=0.14.0        # Granger causality

# ---- Testing ----
pytest>=8.0.0
pytest-cov>=5.0.0
responses>=0.25.0          # Mock HTTP in tests

# ---- Monitoring ----
structlog>=24.0.0
sentry-sdk>=2.0.0
